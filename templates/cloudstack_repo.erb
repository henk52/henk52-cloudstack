# This one will work with the image copied form the DVD.
# The full DVD content is copied to the $basearch and the yum will take care of the rest.
[cloudstack]
name=cloudstack
failovermethod=priority
baseurl=http://<%= @szRepoWebHostAddress %>/images/cloudstack_4.4
enabled=1
metadata_expire=7d
gpgcheck=0
#gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-fedora-$releasever-$basearch
